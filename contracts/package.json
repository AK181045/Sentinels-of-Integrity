{
  "name": "sentinels-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for Sentinels of Integrity",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network polygon_zkevm_testnet",
    "deploy:local": "npx hardhat run scripts/deploy.js --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0"
  }
}
